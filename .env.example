# Local user name
LOCAL_USER=username

# Remote server configuration
REMOTE_USER=username
REMOTE_HOST=YOUR_REMOTE_SERVER_IP_OR_HOSTNAME

# Comma- or space-separated list of directories to back up
# Here we assume the directories are under the local user's home directory.
# Adjust these paths as needed.
SOURCE_DIRS="/home/username/documents /home/username/projects"

# Email address to receive backup notifications
EMAIL=your@email.com
